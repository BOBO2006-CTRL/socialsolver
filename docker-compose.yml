version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: socialsolver
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
